
Python 3.12.8
pip 25.0

Setup:
1. python3 -m venv .venv
2. source .venv/bin/activate
3. pip install -r requirements.txt


Data:
Unzip local_datasets/paper_objects.zip

Scripts:
bash scripts/generation_sample.sh (for profiles + plans)